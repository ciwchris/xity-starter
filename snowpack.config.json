{
  "scripts": {
    "build:css": "postcss",
    "mount:_site": "mount dist --to /",
    "mount:web_modules": "mount web_modules",
    "mount:src": "mount src/assets --to /assets",
    "run:11ty": "eleventy",
    "run:11ty::watch": "$1 --watch"
  },
  "installOptions": {},
  "devOptions": {}
}
