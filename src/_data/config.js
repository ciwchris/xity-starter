const siteConfig = require('../../site.config')

module.exports = {
  name: siteConfig.name,
  shortDesc: siteConfig.shortDesc,
  url: siteConfig.url,
  lang: siteConfig.lang,
  authorHandle: siteConfig.authorHandle,
  authorName: siteConfig.authorName,
  paymentPointer: siteConfig.paymentPointer,
  syntaxTheme: siteConfig.syntaxTheme,
}
